schema {
    query: Query
}

type Query {
    native: String
    private(int: Int, new: String, enum: TestEnum = long): Synchronized
    case(final: [char]): ID
}

type Synchronized {
    void: String
    wait: char
}

enum TestEnum {
    long,
    short
}

interface char {
}