version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    working_directory: ~/repo/graphql-java-codegen
    environment:
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      - run: date +%Y-%m > .circle-cache-key
      - restore_cache:
          keys:
            - deps-{{checksum ".circle-cache-key"}}
      - run: gradle install
      - save_cache:
          paths:
            - ~/.gradle
            - ~/.m2
          key: deps-{{checksum ".circle-cache-key"}}-{{ .Environment.CIRCLE_SHA1 }}-{{ .Environment.CIRCLE_WORKFLOW_ID }}
      - run: gradle codeCoverageReport
      - run: bash <(curl -s https://codecov.io/bash)
  build-graphql-java-codegen-maven-plugin:
    docker:
      - image: circleci/openjdk:8-jdk
    working_directory: ~/repo/graphql-java-codegen-maven-plugin
    environment:
      MAVEN_OPTS: -Xmx3200m
    steps:
      - checkout
      - run: date +%Y-%m > .circle-cache-key
      - restore_cache:
          keys:
            - deps-{{checksum ".circle-cache-key"}}-{{ .Environment.CIRCLE_SHA1 }}-{{ .Environment.CIRCLE_WORKFLOW_ID }}
            - deps-{{checksum ".circle-cache-key"}}
      - run: cd plugins/graphql-java-codegen-maven-plugin && mvn dependency:go-offline
      - save_cache:
          paths:
            - ~/.gradle
            - ~/.m2
          key: deps-{{checksum ".circle-cache-key"}}
      - run: cd plugins/graphql-java-codegen-maven-plugin && mvn package
  build-graphql-java-codegen-gradle-plugin:
    docker:
      - image: circleci/openjdk:8-jdk
    working_directory: ~/repo/graphql-java-codegen-gradle-plugin
    environment:
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      - run: date +%Y-%m > .circle-cache-key
      - restore_cache:
          keys:
            - deps-{{checksum ".circle-cache-key"}}-{{ .Environment.CIRCLE_SHA1 }}-{{ .Environment.CIRCLE_WORKFLOW_ID }}
            - deps-{{checksum ".circle-cache-key"}}
      - run: cd plugins/graphql-java-codegen-gradle-plugin && gradle dependencies
      - save_cache:
          paths:
            - ~/.gradle
            - ~/.m2
          key: deps-{{checksum ".circle-cache-key"}}
      - run: cd plugins/graphql-java-codegen-gradle-plugin && gradle test
workflows:
  build-library-and-plugins:
    jobs:
      - build
      - build-graphql-java-codegen-maven-plugin:
          requires:
            - build
      - build-graphql-java-codegen-gradle-plugin:
          requires:
            - build
  version: 2